/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The demo is opened through 127.0.0.1 in Codex's in-app browser.
  allowedDevOrigins: ["127.0.0.1", "localhost"],
};
export default nextConfig;
